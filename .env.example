# 网站URL（用于SEO和元数据）
NEXT_PUBLIC_SITE_URL=https://knowyourself.com.cn

# 开发环境下可以使用 localhost
# NEXT_PUBLIC_SITE_URL=http://localhost:3001

# 数据库连接（PostgreSQL）
# 格式：postgresql://用户名:密码@主机:端口/数据库名
# 生产环境示例：
DATABASE_URL="postgresql://scales_user:your_password@localhost:5432/scales_db"

# 开发环境可以使用相同配置，或者使用 SQLite 进行本地测试：
# DATABASE_URL="file:./dev.db"
